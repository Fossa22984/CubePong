public enum PlayerPosition
{
    Left = -90,
    Bottom = 0,
    Right = 90,
    Top = 180
}

public enum TypeCoin
{
    Small,
    Average,
    Big
}

public enum GameState
{
    MainMenu,
    Game,
    GameOverMenu
}

public enum SfxType
{
    Jump,
    GetCoin,
    Death
}

public enum MusicType
{
    MainGameMusic
}